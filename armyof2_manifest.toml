# Generated by ReXGlue v0.10.0.2-dev.gc94f5eb on 2026-09-08 16:18:54 UTC.

[project]
name = "armyof2"
sdk_version = "0.10.0"
game_root = "../artifacts/armyoftwoextracted"

[entrypoint]
file_path = "../artifacts/armyoftwoextracted/default.xex"
out_directory_path = "generated/default"
includes = []

# NOTE: recompiler register-localization codegen opts (skip_lr, ctr/xer/cr/
# non_volatile/reserved/non_argument_as_local) were tested and reverted. The
# aggressive cross-function-register opts hang this game (tail-call thunks +
# speculative scan functions have imprecise boundaries); the safe subset ran but
# gave ~0 FPS because the bottleneck is per-draw cost in the emulator's IssueDraw
# (~80us/draw x ~600 draws/frame), not guest instruction overhead. See the
# armyof2-60fps-patch memory note. Left at defaults (all false).

# Function-boundary hints for branch targets the analyzer did not register as
# function entry points (all surfaced as UnresolvedCall on the first codegen pass).
# Nested under [entrypoint] because the parser reads `functions` from this binary's
# own config table. Address-only declarations let normal boundary discovery run.
[entrypoint.functions]
# Alternate entry into the tiny thunk sub_82CAB890 (li r6,0; b 0x82cae338).
# 0x82CAB898 skips the `li r6,0` and tail-branches to sub_82CAE338 with r6 as the
# caller left it. 18 call sites branch straight here.
"0x82CAB898" = { name = "sub_82CAB898" }
# Target of the pure thunk sub_82897720 (b 0x82734628); lands inside sub_82734610.
"0x82734628" = { name = "sub_82734628" }
# Target of the pure thunk sub_82EA2FD8 (b 0x82bc3110).
"0x82BC3110" = { name = "sub_82BC3110" }
# Runtime-surfaced: indirect-call (bctr vtable) target with no registered entry.
# Small function right after sub_82B50E78's bctr, before sub_82B50EB0; reached
# only via vtable dispatch, so static discovery/GapFill never emitted it.
"0x82B50E98" = { name = "sub_82B50E98" }
# Runtime-computed indirect target (address not in static data, so the
# data-pointer scan can't see it); sits in a gap after sub_82DD6BB0.
"0x82DD6D18" = { name = "sub_82DD6D18" }
